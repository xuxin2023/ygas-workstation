# RC Triage Rules

## 适用范围

- 本规则用于当前 RC 现场试用阶段的问题分级、继续试用判断和修复优先级统一。
- 进入下一轮 RC 时，允许修复现场问题、补测试、补文档和补打包一致性校验。
- 不允许借机改协议解析格式、改命令语义、放宽安全限制或改变导出字段口径。

## 分级规则

### P0

- 误写设备。
- `FFF` 广播误触发。
- 严格只读、会话模式、权限等级或目标设备一致性失效，导致本应阻止的写命令被放行。
- 崩溃、卡死或数据丢失导致现场无法继续。
- 默认主流程失效，连接后无法形成可用实时数据闭环。

处置要求：

- 立即停止现场试用。
- 立即导出会话包、命令日志、参数变更记录和截图。
- 当天完成初步定级、归因和修复安排。

### P1

- 实时流可以启动，但状态提示或诊断闭环缺失，导致现场无法快速定位问题。
- 关键状态显示错误，可能误导操作。
- 关键留证资料缺失，导致问题无法复盘。
- 长时间采集、导出或回放存在稳定可复现异常。

处置要求：

- 当天登记并留证。
- 48 小时内给出修复方向或现场规避方案。

### P2

- 文档不够清晰。
- 页面可用但在小屏或缩放场景下体验不佳。
- 非关键提示文案、布局或步骤可优化。

### P3

- 排版、命名或帮助文档细节优化。

## 留证要求

- 任何 `P0 / P1` 都必须导出会话包。
- 涉及主动上传、恢复失败、ACK-only、`FFF` 广播、严格只读或目标设备不一致的场景，必须保留截图与命令日志。
- 问题登记优先使用当前版本配套的 `FIELD_TRIAL_ISSUE_TEMPLATE_v当前版本.md`。

## 回归要求

- 所有 `P0 / P1` 修复后都必须补自动化测试或明确人工回归步骤。
- 回归优先覆盖：连接后自动启动实时流、首帧超时诊断、原始 RX 解析失败提示、严格只读/严格只听拦截、`FFF` 广播拦截、会话包/诊断包/CSV 导出一致性。
