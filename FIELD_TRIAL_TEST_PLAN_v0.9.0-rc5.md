# Field Trial Test Plan v0.9.0-rc5

## 试用前准备

- 按 `README_FIELD_TRIAL.md` 和 `FIELD_TRIAL_QUICK_CARD_v0.9.0-rc5.md` 先完成资料校验。
- 核对 FieldTrial 顶层 `SHA256SUMS.txt`。
- 核对外部 `.sha256` 文件。
- 发布前复核见 `RELEASE_CHECKLIST_v0.9.0-rc5.md`。

## 核心场景

### 场景 1：默认实时监测主流程

1. 保持默认实时监测模式。
2. 采集方式设为自动上传 / `LISTEN`。
3. 解析模式保持 `AUTO`。
4. 连接真实设备。
5. 观察是否自动发送 `SETCOMWAY=1` 并出现曲线。

### 场景 2：首帧超时诊断

1. 让设备 ACK 成功但暂不输出有效气体帧。
2. 确认页面出现“主动上传已开启，但尚未收到有效气体帧”提示。
3. 使用“重新启动实时流 / 诊断”继续定位；“诊断”内含切换解析模式、读取关键配置和打开串口日志。

### 场景 3：收到原始 RX 但解析失败

1. 保持串口有原始 RX 数据。
2. 让当前解析模式与设备输出格式不匹配。
3. 确认页面出现“收到串口数据，但未解析为有效气体帧”提示。

### 场景 4：安全边界

1. 打开严格只读，确认不会自动发送。
2. 切到严格只听，确认不会自动发送。
3. 切到手动读取，确认不会自动发送。
4. 目标设为 `FFF`，确认不会自动广播启动主动上传。

### 场景 5：小屏与缩放

1. 在 1280x720、1366x768、1600x900 下查看主要页面。
2. 确认设备控制、系数中心、信号参数、专家页、导出页都能滚动到底部。

## 记录要求

- 每天填写 `FIELD_TRIAL_DAILY_LOG_v0.9.0-rc5.md`
- 备注记录写入 `FIELD_TRIAL_NOTES_v0.9.0-rc5.md`
- 问题使用 `FIELD_TRIAL_ISSUE_TEMPLATE_v0.9.0-rc5.md`
